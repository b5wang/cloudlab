server:
  port: 8007
spring:
  application:
    name: scex-shipping
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/scex-shipping
    username: root
    password: root
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    hibernate:
      ddl-auto: none
logging:
  level:
    org.springframework.data: DEBUG